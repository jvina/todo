import React from "react"
import { Todo } from "../models/model";

interface props {
    todos: Array<Todo>
}
const Todos: React.FC<props> = ({
    todos
    
}) => {
    return (
        <div>
            
        
        </div>
    );
}

export default Todos